import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { theme } from '@refract-ui/core';
import BreadCrumb from '../../components/BreadCrumb';
import ColorPalette from '../../components/ColorPalette';

[//]: # (naming convention is core/theme/tokenNameFromThemeDiagram for breadcrumbs to work)
<Meta
  title="core/theme/example"
/>

[//]: # (generic template with args)
export const Template = (args) => {
  const { colors } = theme({
    colors: ({ defaults }) => ({
      ...defaults,
      ...args
    })
  });
  return <ColorPalette colors={colors} />;
}

[//]: # (generic template with no args)
export const FunctionTemplate = () => {
  const { colors } = theme({
    colors: ({ defaults }) => ({
      ...defaults,
      white: '#fefefe',
      brand: 'purple'
    })
  });
  return <ColorPalette colors={colors} />;
}

<BreadCrumb token="example" />

# Colors

[//]: # (example of binding args to template)
<Canvas>
  <Story
    name="colors"
    argTypes={{
      black: {
        control: {
          type: 'color'
        }
      }
    }}
    args={{
      black: '#000000'
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Description

DESCRIPTION OF EXAMPLE GOES HERE

## Default Props

[//]: # (display default return value for given function or set of tokens)

``` js
import { theme } from '@refract-ui/core';
const { colors } = theme({});

assert(colors).isEqual({
  black: '#000000',
  blue: '#3978EC',
  cyan: '#17a2b8',
  gray: '#B0B9C9',
  green: '#6AD193',
  indigo: '#6610f2',
  orange: '#fd7e14',
  pink: '#d63384',
  purple: '#6f42c1',
  red: '#DE7283',
  teal: '#20c997',
  white: '#ffffff',
  yellow: '#EFC174'
});
```

## Theme Configuration

`colors` can be configured by extending the global styles via function or with static values. In each example, we replace the `colors.primary` with a new color and extend the `colors` object with a new key-value pair.

### Using Function Values

[//]: # (replace template with actual example)

<Canvas showCode>
  <Story name="colorsFunction">
    {FunctionTemplate.bind({})}
  </Story>
</Canvas>

``` js
import { theme } from '@refract-ui/core';

const { colors } = theme({
  colors: ({ defaults }) => ({
    ...defaults,
    white: '#fefefe',
    brand: 'purple'
  })
});
```

### Using Static Values

[//]: # (same as above, but with static values)

## All Configurable Props

[//]: # (for use if props are more complicated than default props)

- base color configuration
