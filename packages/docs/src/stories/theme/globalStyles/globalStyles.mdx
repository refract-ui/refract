# Global styles

import { Meta, Story } from '@storybook/addon-docs';
import BreadCrumb from '../../../components/BreadCrumb';
import { DefaultTemplate, WithSubTheme } from './globalStyles.stories';

<BreadCrumb
  token="globalStyles"
/>

The `globalStyles` attribute is used to define the default styles for all the native html tags. The values defined here are derived from the previously defined higher-level theme settings;

## Default Props

<Story
  name="globalStyles"
  id="core-theme-global-styles--default-template"
>
  {DefaultTemplate.bind({})}
</Story>

### Theme Nesting

<Story
  name="subTheme"
  id="core-theme-global-styles--with-sub-theme"
>
  {WithSubTheme.bind({})}
</Story>
