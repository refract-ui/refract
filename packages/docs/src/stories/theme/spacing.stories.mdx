import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { theme } from '@refract-ui/core';
import BreadCrumb from '../../components/BreadCrumb';
import ColorPalette from '../../components/ColorPalette';
import Spacing from '../../components/Spacing';

<Meta
  title="core/theme/spacing"
/>

export const Template = (args) => {
  const { spacing } = theme({
    spacing: ({ defaults }) => ({
      ...defaults,
      ...args
    })
  });
  return <Spacing spacing={spacing} />;
}

export const FunctionTemplate = () => {
  const { spacing } = theme({
    spacing: ({ defaults }) => ({
      ...defaults,
      3: '1.25rem',
      6: '6rem'
    })
  });
  return <Spacing spacing={spacing} />;
}

export const StaticTemplate = () => {
  const { spacing } = theme({
    spacing: {
      3: '1.25rem',
      6: '6rem'
    }
  });
  return <Spacing spacing={spacing} />;
}

<BreadCrumb token="spacing" />

# Spacing

The `spacing` attribute is a set of values that determines the spacing of various components including block elements, inputs, buttons, and tables.

<Canvas>
  <Story
    name="Example"
    argTypes={{
      basis: {
        control: {
          type: 'number'
        }
      }
    }}
    args={{
      basis: 1
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Description

DESCRIPTION OF EXAMPLE GOES HERE

## Default Props

``` js
import { theme } from '@refract-ui/core';
const { spacing } = theme({});

assert(spacing).isEqual({
  0: "0rem",
  1: "0.25rem",
  2: "0.5rem",
  3: "1rem",
  4: "1.5rem",
  5: "3rem",
  basis: 1
});
```

## Theme Configuration

`spacing` can be configured by extending the global styles via function or with static values. In each example, we replace the `spacing[3]` with a new value and extend the `spacing` object with a new key-value pair.

### Using Function Values

<Canvas showCode>
  <Story name="Spacing Function">
    {FunctionTemplate.bind({})}
  </Story>
</Canvas>

``` js
import { theme } from '@refract-ui/core';

const { spacing } = theme({
  spacing: ({ defaults }) => ({
    ...defaults,
    3: '1.25rem',
    6: '6rem'
  })
});
```

### Using Static Values

<Canvas showCode>
  <Story name="Spacing Static">
    {StaticTemplate.bind({})}
  </Story>
</Canvas>

``` js
import { theme } from '@refract-ui/core';

const { spacing } = theme({
  spacing: {
    3: '1.25rem',
    6: '6rem'
  }
});
```
